---
layout: page
---

<div class="post">
  <article>
    {% assign profiles_folder = "_pages/members/" %}
    {% assign members = site.pages | where: "dir", profiles_folder %} <!-- Filter for member files in the specific directory -->

    {% if members.size > 0 %}
      {% for profile in members %}
        <hr>
        <div class="profile float-right">
          {% if profile.image %}
            {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
            <img src="{{ profile_image_path }}" class="img-fluid z-depth-1 rounded-circle" alt="{{ profile.name }}">
          {% endif %}
          
          <div class="clearfix">
            <h2>{{ profile.name }}</h2>
            <p>Tags: {{ profile.tags | join: ', ' }}</p>
            {{ profile.content | markdownify }}
          </div>
        </div>
      {% endfor %}
    {% else %}
      <p>No profiles found.</p> <!-- Debugging line -->
    {% endif %}
  </article>
</div>

